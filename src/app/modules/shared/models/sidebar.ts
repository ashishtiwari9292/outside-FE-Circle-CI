export interface SideBar{
  followersCount: number;
  followingCount: number;
  isFollowed?: boolean;
  fullName: string;
  id: string;
  postCount: number;
  profilePic?: string;
  coverPic?: string;
  username: string;
}